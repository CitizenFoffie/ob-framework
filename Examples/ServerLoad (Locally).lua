-- assuming this is inside of ServerScriptService

local obFramework = require(script.Parent:WaitForChild("MainModule")) -- change MainModule to what you named the script, or if you didn't rename, keep it as it is.

obFramework:Load("2015E")
